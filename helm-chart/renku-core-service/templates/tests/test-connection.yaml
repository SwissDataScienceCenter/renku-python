apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "renku-core-service.fullname" . }}-test-connection"
  labels:
{{ include "renku-core-service.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "renku-core-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
